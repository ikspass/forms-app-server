{
    "version": 2,
    "builds": [{ "src": "./dist/main.js", "use": "@vercel/node" }],
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" },
        { "src": "/(.*)", "dest": "/"}
    ],
    "outputDirectory": "dist"
}